# Generated knowledge

Ask the model to generate relevant background knowledge before tackling the main task.

**Use when:** the task requires domain context the model should surface before reasoning.

**Gap it fixes:** the model answering without surfacing relevant facts that should constrain the answer.

**Example:** `Before answering, list the key facts about [topic] that are relevant to this question. Now use those facts to answer: [question].`