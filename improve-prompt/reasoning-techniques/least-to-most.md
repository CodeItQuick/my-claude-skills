# Least-to-most

Decompose a complex problem into simpler sub-problems and solve them in order.

**Use when:** the task has a clear dependency structure where earlier answers feed later ones.

**Gap it fixes:** the model failing on hard tasks it could solve if broken into steps.

**Example:** `First answer: [simpler sub-question]. Using that answer, now answer: [harder sub-question].`