# Zero-shot

The model receives only an instruction with no examples.

**Use when:** the task is simple and unambiguous.

**Gap it fixes:** over-engineering — adding examples to a task that doesn't need them adds noise.

**Example:** `Translate the following text to French: {text}`