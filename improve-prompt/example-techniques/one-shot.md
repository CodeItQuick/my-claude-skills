# One-shot

A single example before the real request.

**Use when:** the pattern is simple enough that one example is sufficient, but zero-shot produces inconsistent output.

**Gap it fixes:** format ambiguity without the overhead of multiple examples.