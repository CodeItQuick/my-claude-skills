# Structured output

Request output in a specific machine-readable or consistently parseable format.

**Use when:** the output will be parsed, compared, or fed into another system.

**Gap it fixes:** prose output where structured data was needed.

**Example:** `Return your answer as a JSON object with keys: "summary", "confidence", "next_steps".`