# Constraint specification

Explicitly state length, format, audience, tone, and exclusion constraints.

**Use when:** the current prompt leaves any of these implicit, causing inconsistent output.

**Gap it fixes:** the model making assumptions about scope, length, or audience that don't match intent.

**Example:** `Answer in 3 bullet points. Write for a non-technical audience. Do not include code examples.`